apiVersion: v1
kind: Pod
metadata:
  name: test1
spec:
  containers:
  - name: test1-ubuntu
    image: ubuntu
    resources:
      requests:
        memory: "500Mi"
      limits:
        memory: "600Mi"
